# Credenciales de Firebase: el JSON del service account en UNA sola línea.
FIREBASE_SERVICE_ACCOUNT=

# Redis para de-duplicación persistente (OPCIONAL).
# Si no se define, el servicio usa memoria local (funciona igual, pero pierde
# el estado de de-dup al reiniciar).
REDIS_URL=
